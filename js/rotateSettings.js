/**
 * 
 * Defines settings for the rotation of the cube group in the 3D scene.
 * Includes a flag for enabling/disabling rotation animation and the initial rotation value.
 *
 * NOTE: The rotate slider in index.html expects a value of -45, 0, or 45.
 * 
 * It is imported and used in rotateUI.js.
 */
export const rotateSettings = {
  doAnimateRotation: false,  // Flag to enable/disable rotation animation.
  initialRotation: 0,  // The initial rotation angle of the cube group.
};
